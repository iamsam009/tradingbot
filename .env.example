# SharkExchange.in API Credentials
# Create API keys at https://sharkexchange.in → Profile → API Management
# The secret is shown ONLY ONCE during creation — save it securely.
# If empty, bot runs in PAPER mode (real prices, simulated orders).

SHARK_API_KEY=your_api_key_here
SHARK_API_SECRET=your_api_secret_here